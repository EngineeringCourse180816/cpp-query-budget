#include "BudgetDao.h"



BudgetDao::BudgetDao()
{
}


BudgetDao::~BudgetDao()
{
}

Budgets BudgetDao::findAll()
{
	//m_budgets.insert(make_pair(year_month_day_last((year)2018, (month_day_last)(month)1), 370));
	//m_budgets.insert(make_pair(year_month_day_last((year)2018, (month_day_last)(month)2), 370));
	//m_budgets.insert(make_pair(year_month_day_last((year)2018, (month_day_last)(month)3), 370));
	//m_budgets.insert(make_pair(year_month_day_last((year)2018, (month_day_last)(month)4), 370));
	return m_budgets;
}
